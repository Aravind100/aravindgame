
   	function multip(){
        var total=0,num1,num2,p1num,p2num,p1final,p2final ;

        var player1 = prompt("Enter yr name");
        var player2 = prompt("Enter yr name");

        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        
       

       //first attempt
       
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }       
        //second attempt
       
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //third attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //forth attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //5 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //6 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //7 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //8 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //9 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //10 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //11 attempt
       
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
       //12 attempt
       
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }        
        //13 attempt
       
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //14 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //15 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //16 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //17 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //18 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //19 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //20 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //21 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //22 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //23 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //24 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        //25 attempt
        
        p1num = prompt(player1 +" Enter yr no =");
        num1 = parseInt(p1num);
        if(num1<0 || num1>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num1;
        p1final = total;
        alert("Total is ="+total);

        if(p1final >= 100){
            alert(player1 + " win the game");
            return;
        }



        p2num = prompt(player2 +" Enter yr no =");
        num2 = parseInt(p2num);
        if(num2<0 || num2>10){
            alert("no should be in between 1-10");
            return false;
        }
        var total = total+num2;
        p2final = total;
        alert("Total is ="+total);

        if(p2final >= 100){
            alert(player2 + " win the game");
            return;
        }
        
        }
